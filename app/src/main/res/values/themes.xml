<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base app theme using Material3 -->
    <style name="Theme.Linksi" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Splash Screen Theme to prevent flickering on startup -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">?android:attr/colorBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/linksi</item>
        <item name="postSplashScreenTheme">@style/Theme.Linksi</item>
    </style>

    <!-- Transparent theme for ShareReceiverActivity so it looks like a bottom sheet over any app -->
    <style name="Theme.Linksi.Transparent" parent="Theme.Material3.DynamicColors.DayNight">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!--
      Compact dialog-sized window for the optional quick action panel the floating bubble opens.
      It is a normal, focusable activity: the panel must take window focus before it is allowed to
      read the clipboard (specification section 11.1, research note section 7.2), which is exactly
      what android:windowIsFloating and the dimmed background give it. The panel is deliberately
      small so it looks like a popup rather than the whole app, and it never appears unless the
      user tapped the bubble.
    -->
    <style name="Theme.Linksi.QuickPanel" parent="Theme.Material3.DayNight.Dialog">
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
